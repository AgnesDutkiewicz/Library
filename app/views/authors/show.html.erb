<h1> <%= @author.name %> </h1>

<p>
  Birth year:
  <%= @author.birth_year %>
</p>
<p>
  <%= "Book".pluralize(@books.size) %>:
</p>
<% @books.each do |book| %>
  <p>
    <%= link_to book.title, book_path(book) %>
  </p>
<% end %>


<%= link_to "Edit", edit_author_path %>
