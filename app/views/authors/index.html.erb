<%= link_to "Add New Author", new_author_path if policy(Author).create? %>

<h1>Authors</h1>
<p>Authors: <%= @authors.count %> </p>

<% @authors.each do |author| %>
  <p>
    <%= link_to author.name, author %>
  </p>
<% end %>
