<h1> Reservation for <%= @reservation.book.title %> </h1>

<p>
  User: <%= link_to @reservation.user.name, user_path(@reservation.user) %>
</p>
<p>
  Book status:
    <%= @reservation.status %>
</p>

<%= button_to "Change Reservation", edit_book_reservation_path(@reservation.book, @reservation), method: :get %>
